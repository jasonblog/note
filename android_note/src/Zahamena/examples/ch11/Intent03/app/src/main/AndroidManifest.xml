<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.macdidi.intent03">

    <!-- 加入直接撥電話的授權 -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Intent03Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="PhonesActivity" android:label="PhonesActivity"/>
        <activity android:name="MmsActivity" android:label="MmsActivity"/>
        <activity android:name="WebsActivity" android:label="WebsActivity"/>
        <activity android:name="MapsActivity" android:label="MapsActivity"/>
    </application>

</manifest>