1. register.c
  内核模块，通过/proc/registers可以查看相关的寄存器值。
2. dram.c
  内核模块，字符设备，用于访问物理内存，可以通过/dev/dram访问。
3. address.c
  打印CS、DS、ES等寄存器相关值，并打印/proc/registers的值。
4. fileview.c
  用于实时访问/dev/dram。
5. readmem.c
  可用于命令行访问。
