<resources>
    <!-- 只能設定 Material Design 的風格 -->
    <!-- 不可以設定支援套件的 Theme.AppCompat 風格 -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light">
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorTextColorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorWindowBackground</item>
        <item name="android:navigationBarColor">@color/colorNavigationBarColor</item>

        <!-- 啟用轉換動畫 -->
        <item name="android:windowContentTransitions">true</item>

        <!-- 設定進入與離開動畫效果 -->
        <item name="android:windowEnterTransition">
            @transition/explode_transition
        </item>
        <item name="android:windowExitTransition">
            @transition/explode_transition
        </item>
    </style>
</resources>